{
  "name": "portal-unisangil-yopal",
  "version": "1.0.0",
  "description": "Portal Ingeniería de Sistemas — UNISANGIL Sede Yopal",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"blue,cyan\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "postinstall": "npm run install:all",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
